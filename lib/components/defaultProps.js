const defaultProps = {
  accessor: 'attribute',
  columnWidth: 100,
  onColumnClick: (row, column) => {},
  numStickyColumns: 1,
  sortDescending: false,
  stickyHeader: true
};

export default defaultProps;
